spring:
  application:
    name: highton
  datasource:
    url: jdbc:mysql://localhost:3306/youduck
    username: root
    password:
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
server:
  port: 31111

ai:
  server:
    host: localhost
    port: 5000